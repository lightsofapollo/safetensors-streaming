# Rust build artifacts (don't sync to GPU pod — compile on pod instead)
crates/target/
tools/*/target/

# Python build artifacts
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
*.so
# Keep dist/*.whl for prebuilt wheels (cross-compiled for GPU pod)
!dist/*.whl

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Beads
.beads/dolt/

# Virtual environments (don't sync to GPU pod)
.venv/
venv/

# Environment
.env
.gpu/
benches/results/.tmp*
